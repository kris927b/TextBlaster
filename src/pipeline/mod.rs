// src/pipeline/mod.rs

pub mod filters;
pub mod readers;
pub mod token;
pub mod writers; // Added new module

// Potentially other pipeline related items, traits, etc.
// e.g., pub trait PipelineStep {}
// pub struct Pipeline {}
